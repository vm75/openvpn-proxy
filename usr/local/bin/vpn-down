#!/bin/sh

. $(dirname $0)/common.inc

function main() {
    loadSavedVars

    # User apps
    if [ -f /data/apps.sh ] ; then
        sh /data/apps.sh down &
    fi

    # log "Killing socks proxy"
    # pkill -9 -x sockd
    log "Killing http proxy"
    pkill -9 -x tinyproxy
}

main "$@"

exit 0
